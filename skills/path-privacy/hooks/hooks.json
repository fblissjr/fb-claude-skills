{
  "description": "path-privacy hooks: SessionStart (startup only) refreshes stale git hook wrappers and is silent otherwise; PreToolUse rewrites in-repo absolute paths to repo-relative and blocks external paths and the git user.name full name in Write/Edit content and git/gh commands (the git hooks remain the authoritative gate).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "bash",
            "args": [
              "${CLAUDE_PLUGIN_ROOT}/hooks/path-privacy-session-start.sh"
            ]
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash",
            "timeout": 30,
            "args": [
              "${CLAUDE_PLUGIN_ROOT}/hooks/path-privacy-pre-tool-use.sh"
            ]
          }
        ]
      }
    ]
  }
}
