# trigger: any
## TDD workflow (auto-loaded)
- TDD: ALWAYS write a failing test first, then implement, then refactor. No exceptions for behavioral changes.
- Run tests after every change. Never skip the red step.
- Every new test records its claim: a one-line note of what breaks if it is deleted.
- For full TDD methodology, invoke /dev-conventions:tdd-workflow.
