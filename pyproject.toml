[project]
name = "fb-claude-skills"
version = "0.17.0"
description = "Plugin marketplace and extension system for Claude Code"
license = "MIT"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["skill-maintainer", "env-forge", "skill-dashboard", "mece-decomposer"]

[dependency-groups]
dev = ["pytest>=7.0", "ruff>=0.8.0"]
