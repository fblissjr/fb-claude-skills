[project]
name = "env-forge"
version = "0.2.0"
description = "Database-backed MCP tool environment generator"
requires-python = ">=3.11"
dependencies = ["orjson>=3.10", "huggingface-hub>=1.4.1"]

[tool.uv]
package = false
