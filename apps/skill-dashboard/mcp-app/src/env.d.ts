/**
 * Injected by vite.config.ts from .claude-plugin/plugin.json at build time.
 *
 * Declared rather than authored: the value has exactly one source, and a second
 * hand-maintained copy of a version string is what this replaced.
 */
declare const __APP_VERSION__: string;
