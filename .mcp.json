{
  "_comment": "Root MCP server config. Servers here are loaded automatically by Claude Code when running in this repo. Global MCP servers live in the user's Claude config directory; keep this file scoped to this repo.",
  "mcpServers": {}
}
