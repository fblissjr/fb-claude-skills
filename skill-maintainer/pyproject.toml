[project]
name = "skill-maintainer"
version = "0.1.0"
description = "Maintenance tooling for Agent Skills"
requires-python = ">=3.11"
dependencies = ["orjson>=3.10", "httpx>=0.27", "skills-ref"]

[tool.uv]
package = false
